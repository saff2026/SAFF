{
  "rules": {
    "portas": {
      ".read": "auth != null",
      "users": {
        "$key": {
          ".write": "auth != null && newData.exists()",
          "name": {
            ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 30"
          },
          "ph": {
            ".validate": "!data.exists() || newData.val() === data.val()"
          }
        }
      },
      "predictions": {
        "$emp": {
          "$key": {
            ".write": "auth != null",
            ".validate": "newData.isString() && (newData.val() === 'up' || newData.val() === 'out' || newData.val() === 'stay')"
          }
        }
      },
      "results": {
        "$emp": {
          ".write": "auth != null",
          ".validate": "newData.isString() && (newData.val() === 'up' || newData.val() === 'out' || newData.val() === 'stay')"
        }
      },
      "excluded": {
        "$emp": {
          ".write": "auth != null",
          ".validate": "newData.isBoolean()"
        }
      },
      "section": {
        "$emp": {
          ".write": "auth != null",
          ".validate": "newData.isString() && newData.val().length <= 40"
        }
      },
      "doubles": {
        "$key": {
          ".write": "auth != null",
          ".validate": "newData.isString() && newData.val().length <= 20"
        }
      }
    },
    "wc2026": {
      ".read": "auth != null",
      "users": {
        "$key": {
          ".write": "auth != null && newData.exists()",
          "name": {
            ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 30"
          },
          "ph": {
            ".validate": "!data.exists() || newData.val() === data.val()"
          }
        }
      },
      "predictions": {
        "$mid": {
          "$key": {
            ".write": "auth != null",
            ".validate": "newData.hasChildren(['h','a']) && newData.child('h').isNumber() && newData.child('a').isNumber() && newData.child('h').val() >= 0 && newData.child('h').val() <= 30 && newData.child('a').val() >= 0 && newData.child('a').val() <= 30"
          }
        }
      },
      "results": {
        "$mid": {
          ".write": "auth != null",
          ".validate": "newData.hasChildren(['h','a']) && newData.child('h').isNumber() && newData.child('a').isNumber() && newData.child('h').val() >= 0 && newData.child('a').val() >= 0"
        }
      },
      "teams": {
        "$mid": {
          ".write": "auth != null"
        }
      },
      "rooms": {
        "$rid": {
          ".write": "auth != null"
        }
      }
    },
    "saff2026": {
      ".read": "auth != null",
      "users": {
        "$key": {
          ".write": "auth != null && newData.exists()",
          "name": {
            ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 30"
          },
          "ph": {
            ".validate": "!data.exists() || newData.val() === data.val()"
          }
        }
      },
      "predictions": {
        "$mid": {
          "$key": {
            ".write": "auth != null",
            ".validate": "newData.hasChildren(['h','a']) && newData.child('h').isNumber() && newData.child('a').isNumber() && newData.child('h').val() >= 0 && newData.child('h').val() <= 30 && newData.child('a').val() >= 0 && newData.child('a').val() <= 30"
          }
        }
      },
      "results": {
        "$mid": {
          ".write": "auth != null",
          ".validate": "newData.hasChildren(['h','a']) && newData.child('h').isNumber() && newData.child('a').isNumber() && newData.child('h').val() >= 0 && newData.child('a').val() >= 0"
        }
      },
      "teams": {
        "$mid": {
          ".write": "auth != null"
        }
      },
      "rooms": {
        "$rid": {
          ".write": "auth != null"
        }
      },
      "polls": {
        "$pid": {
          "$key": {
            ".write": "auth != null",
            ".validate": "newData.isString() && newData.val().length <= 4"
          }
        }
      }
    },
    "gulf27": {
      ".read": "auth != null",
      "users": {
        "$key": {
          ".write": "auth != null && newData.exists()",
          "name": {
            ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 30"
          },
          "ph": {
            ".validate": "!data.exists() || newData.val() === data.val()"
          }
        }
      },
      "predictions": {
        "$mid": {
          "$key": {
            ".write": "auth != null",
            ".validate": "newData.hasChildren(['h','a']) && newData.child('h').isNumber() && newData.child('a').isNumber() && newData.child('h').val() >= 0 && newData.child('h').val() <= 30 && newData.child('a').val() >= 0 && newData.child('a').val() <= 30"
          }
        }
      },
      "results": {
        "$mid": {
          ".write": "auth != null",
          ".validate": "newData.hasChildren(['h','a']) && newData.child('h').isNumber() && newData.child('a').isNumber() && newData.child('h').val() >= 0 && newData.child('a').val() >= 0"
        }
      },
      "teams": {
        "$mid": {
          ".write": "auth != null"
        }
      },
      "rooms": {
        "$rid": {
          ".write": "auth != null"
        }
      },
      "polls": {
        "$pid": {
          "$key": {
            ".write": "auth != null",
            ".validate": "newData.isString() && newData.val().length <= 4"
          }
        }
      },
      "live": {
        "$mid": {
          ".write": "auth != null"
        }
      },
      "storyResets": {
        "$day": {
          ".write": "auth != null"
        }
      },
      "scorers": {
        "$sid": {
          ".write": "auth != null",
          ".validate": "!newData.exists() || (newData.hasChildren(['name','goals']) && newData.child('name').isString() && newData.child('name').val().length > 0 && newData.child('name').val().length <= 60 && newData.child('goals').isNumber() && newData.child('goals').val() >= 0 && newData.child('goals').val() <= 30)"
        }
      }
    }
  }
}
